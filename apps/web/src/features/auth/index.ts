/**
 * Auth feature public API.
 *
 * Consumers (auth route pages) import from `@/features/auth` rather than from
 * deep paths so internal restructuring stays a one-file change.
 */
export { AuthCard } from './components/auth-card';
export { SsoButtons } from './components/sso-buttons';
